require 'spec_helper'

describe Hot100CLI do
  it 'has a version number' do
    expect(Hot100CLI::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
